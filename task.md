# Danh sách công việc (Task Checklist) - Tích hợp Webhook NVC & Sửa lỗi CSS

- `[x]` Thực hiện **deploy** (đưa lên môi trường chạy thực tế) Google Apps Script Web App mới.
- `[x]` Cập nhật URL **webhook** (đường dẫn nhận phản hồi tự động) vào tệp `register_nvc.html`.
- `[x]` Sửa lỗi **typo** (lỗi đánh máy) CSS `input:focutaos` thành `input:focus` ở tệp `register.html`.
- `[/]` Kiểm tra và đồng bộ hóa mã nguồn lên GitHub.
- `[ ]` Thực hiện **UAT** (User Acceptance Testing - kiểm thử nghiệm thu người dùng) trên trang **live** (trực tuyến) của Vercel và chụp ảnh minh chứng.
